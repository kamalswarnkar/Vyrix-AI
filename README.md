Completed the backend feature as intended.

Built:

Llama 3 model support and default local model config.
Research-assistant system prompt for PhD/research workflows.
RAG pipeline with document chunk storage and retrieval.
Upload API for PDF, PNG, JPEG, text, markdown, HTML.
Real PDF text extraction via pdf-parse.
Real image OCR via tesseract.js.
Workspace context scanner API.
Chat auto-injects workspace context + retrieved document chunks.
SQLite document repository and local project placeholders.
Secure upload validation: allowed MIME types, base64 validation, size cap.
Modern lint/test config.
Dependency security fixes and overrides.
